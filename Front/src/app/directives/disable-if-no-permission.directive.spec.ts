
// Front/src/app/directives/disable-if-no-permission.directive.spec.ts
import { DisableIfNoPermissionDirective } from './disable-if-no-permission.directive';

describe('DisableIfNoPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableIfNoPermissionDirective();
    expect(directive).toBeTruthy();
  });
});